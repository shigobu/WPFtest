<Window x:Name="ProgressBarWin" x:Class="バックアップはできますWPF.ProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:バックアップはできますWPF"
        mc:Ignorable="d"
        Title="計算中" Height="108.109" Width="528.618" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Icon="icon/icon.ico">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0*"/>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="progressBar1" Height="17" Margin="10,10,10.4,0" VerticalAlignment="Top" IsIndeterminate="True"/>
        <Label x:Name="label1" Content="計算中" HorizontalAlignment="Left" Margin="10,27,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.524,-0.324" Height="27" Width="423"/>
        <Button x:Name="ButtonCancel" Content="キャンセル" HorizontalAlignment="Right" Margin="0,0,10.4,10" Width="75" Height="23" VerticalAlignment="Bottom"/>

    </Grid>
</Window>
