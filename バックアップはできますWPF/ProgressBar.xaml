<Window x:Name="ProgressBarWin" x:Class="バックアップはできますWPF.ProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:バックアップはできますWPF"
        mc:Ignorable="d"
        Title="計算中" Height="108.109" Width="528.618" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Icon="icon/icon.ico">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="473*"/>
            <ColumnDefinition Width="49*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="29*"/>
            <RowDefinition Height="11*"/>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="progressBar1" Height="17" Margin="10,10,9.4,0" VerticalAlignment="Top" Grid.ColumnSpan="2" IsIndeterminate="True"/>
        <Label x:Name="label1" Content="計算中" HorizontalAlignment="Left" Margin="10,27,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.524,-0.324"/>
        <Button x:Name="ButtonCancel" Content="キャンセル" HorizontalAlignment="Right" Margin="0,0,10.4,9" Width="75" Height="23" VerticalAlignment="Bottom" Grid.RowSpan="2" Grid.ColumnSpan="2"/>

    </Grid>
</Window>
